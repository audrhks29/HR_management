/** @jsxImportSource @emotion/react */

import { memo } from "react";
import { InnerStyle } from "../styles/commonStyles";

const Download = memo(() => {
  return <section css={InnerStyle}>다운로드 페이지</section>;
});

export default Download;
