import Index from "@/components/salary/Edit/Index";
import { memo } from "react";

const Salary_edit = memo(() => {
  return (
    <main>
      <Index />
    </main>
  );
});

export default Salary_edit;
