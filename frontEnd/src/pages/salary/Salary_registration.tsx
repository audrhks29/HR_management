import Index from "@/components/salary/registration/Index";
import { memo } from "react";

const Salary_registration = memo(() => {
  return (
    <main>
      <Index />
    </main>
  );
});

export default Salary_registration;
