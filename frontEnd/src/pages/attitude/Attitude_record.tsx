import Index from "@/components/attitude/record/Index";
import { memo } from "react";

const Attitude_record = memo(() => {
  return (
    <main>
      <Index />
    </main>
  );
});

export default Attitude_record;
