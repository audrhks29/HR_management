import Index from "@/components/hr/organization/Index";
import { memo } from "react";

const Hr_organization_chart = memo(() => {
  return (
    <main>
      <Index />
    </main>
  );
});

export default Hr_organization_chart;
